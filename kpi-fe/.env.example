# Base URL of the Java Spring Boot backend
VITE_API_BASE_URL=/api/v1

# Set to "true" to use mock adapter (no backend needed).
# Set to "false" (or remove) to call the real backend.
VITE_USE_MOCK=true
